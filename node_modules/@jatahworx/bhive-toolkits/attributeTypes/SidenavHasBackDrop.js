let SidenavHasBackDropInstance = null;
module.exports = class hasBackDrop {

    constructor() {
        if (!SidenavHasBackDropInstance) {
            SidenavHasBackDropInstance = this;
            //set all other default values

            this.displayAs = 'Has Back Drop';
            this.value = '[hasBackdrop]'
            this.type = 'TOGGLE';
            this.values = {'true-value':'true', 'false-value': 'false'}
        }
        return SidenavHasBackDropInstance;
    }
}