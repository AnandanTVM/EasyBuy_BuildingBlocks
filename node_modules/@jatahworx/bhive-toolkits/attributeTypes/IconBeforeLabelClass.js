let autoSizeInstance = null;
module.exports = class IconBeforeLabelClass {

    constructor() {
        if (!autoSizeInstance) {
            autoSizeInstance = this;
            //set all other default values
            this.displayAs = 'IconBeforeLabelClass';
            this.value = '';
            this.type = 'AUTOCOMPLETE';
            this.values = { readFrom: 'CLASSES' };
        }
        return autoSizeInstance;
    }
};