module.exports = class mode {

    constructor(value, values) {
        //set all other default values

        this.displayAs = 'Mode';
        this.value = value;
        this.type = 'DROPDOWN';
        this.values = values;
    }
}