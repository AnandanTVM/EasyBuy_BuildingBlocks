let MatErrorInstance = null;
module.exports = class MatError {

    constructor() {
        if (!MatErrorInstance) {
            MatErrorInstance = this;
            this.value = 'matError'
            this.displayAs = 'Errors';
            this.type = 'LIST_DIALOG';
            this.values = {

            }
            this.title = "Configure Errors"
            this.model = {
                input1: {
                    placeholder: 'Error Condition'
                },
                input2: {
                    placeholder: 'Message'
                },
                toggle: {
                    toggleData: ['start', 'end'],
                    selected: 'start',
                    displayAs: 'Message align'
                }
            }
        }
        return MatErrorInstance;
    }
}
