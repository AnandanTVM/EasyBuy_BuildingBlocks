let chipListSelectableInstance = null;
module.exports = class chipListSelectable {

    constructor() {
        if (!chipListSelectableInstance) {
            chipListSelectableInstance = this;
            //set all other default values

            this.displayAs = 'chipListSelectable';
            this.value = 'chipListSelectable'
            this.type = 'TOGGLE';
            this.values = {'true-value':'true', 'false-value': 'false'}
        }
        return chipListSelectableInstance;
    }
}