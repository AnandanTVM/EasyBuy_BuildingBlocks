let BaseComponent = require("@jatahworx/bhive-toolkits").BaseComponent;

module.exports = class EmailOutConfig extends BaseComponent {
    constructor(constructorOptions) {
        super(constructorOptions.id, constructorOptions.name, "neutrinos_service_designer_EmailOut", "emailout-config");
        this.functionName = constructorOptions.id;
        this.viewType = 'server';
    
        }
}