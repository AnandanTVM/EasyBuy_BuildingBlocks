let BaseComponent = require("@jatahworx/bhive-toolkits").BaseComponent;

module.exports = class TestNode extends BaseComponent {
    constructor(constructorOptions) {

        super(constructorOptions.id, constructorOptions.name, "neutrinos_service_designer_catch", "TestNode");
        this.serviceName = constructorOptions.serviceName;
        this.selectedNodes = constructorOptions.selectedNodes;
        this.viewType = 'common';
        console.log(constructorOptions);
	}
	
	/**
	 * 
	 * generateSnippet can return an object with these options or a plain string:
	 * - noWrap = false,
	 * - async = true,
	 * - argsSnippet = '',
	 * - nodeSnippet = '',
	 * - appendNextNodePlaceholder = true,
	 * - returnSnippet = ''
	 */
    generateSnippet() {
        return `console.log('test node was called');`;
    }

    getCallTemplate() {
        return `bh = await this.${this.functionName}(bh) `;
    }


};