let BaseComponent = require("@jatahworx/bhive-toolkits").BaseComponent;

module.exports = class BasicAuthConfig extends BaseComponent {
    constructor(constructorOptions) {
        super(constructorOptions.id, constructorOptions.name, "neutrinos_service_designer_BasicAuthConfig", "basicauth-config");
        this.functionName = constructorOptions.id;
        this.viewType = 'server';
    }
};