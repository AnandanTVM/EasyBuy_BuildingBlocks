let BaseComponent = require("@jatahworx/bhive-toolkits").BaseComponent;

module.exports = class SoapConfig extends BaseComponent {
    constructor(constructorOptions) {
        super(constructorOptions.id, constructorOptions.name, "neutrinos_service_designer_SoapConfig", "soap-config");
        this.functionName = constructorOptions.id;
        this.viewType = 'server';
    }
};