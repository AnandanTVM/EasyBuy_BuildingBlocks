let BaseComponent = require("@jatahworx/bhive-toolkits").BaseComponent;

module.exports = class AMQPConfig extends BaseComponent {
    constructor(constructorOptions) {
        super(constructorOptions.id, constructorOptions.name, "neutrinos_service_designer_AMQPConfig", "amqp-config");
        this.functionName = constructorOptions.id;
        this.viewType = 'server';
    }
};
