'use strict';
let Attribute = require('../../core/Attributes');
let Component = require('../../core/Component');

module.exports = class Footer extends Component {
  constructor() {
    const name = 'NG-Footer';
    const designerTemplate = `<div class=\"drop\" component-label="Footer"></div>`;
    const paletteTemplate = 'Footer';
    const componentLabel = 'Footer';
    const templateUrl = 'articles/#!components-guide-7/footer';

    super({
      name: name,
      designerTemplate: designerTemplate,
      paletteTemplate: paletteTemplate,
      componentLabel: componentLabel,
      templateUrl: templateUrl
    }
      
    );
    super.setType(Component.COMPONENT_TYPE_TITLES.LAYOUT.val);
  }
  get template() {
    const template = `<footer %style% %class% %bCustomProps%></footer>`;
    return template;
  }
  set template(templateString) {}
};