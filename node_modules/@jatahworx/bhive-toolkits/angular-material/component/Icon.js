'use strict';
let Attribute = require('../../core/Attributes');
let Component = require('../../core/Component');

module.exports = class MDIcon extends Component {
  constructor() {
    const name = 'NG-icon';
    const designerTemplate = `<div class=\"ad-icon non-dropable-comp-border form-control-comp\">
                                    <i class="material-icons component-placeholder">info</i>
                                  </div>`;
    const paletteTemplate = 'icon';
    const componentLabel = 'Icon';
    const templateUrl = 'articles/#!components-guide-7/icon';

    super({
      name: name,
      designerTemplate: designerTemplate,
      paletteTemplate: paletteTemplate,
      componentLabel: componentLabel,
      templateUrl:templateUrl
    });
    super.setType(Component.COMPONENT_TYPE_TITLES.FORM_CONTROLS.val);

    super.addAttribute(
      new Attribute({
        key: 'IconName',
        value: 'home',
        type: 'a',
        useAsLabel: true
      })
    );
    super.addAttribute(
      new Attribute({
        key: '[inline]',
        value: '',
        type: 'kv'
      })
    );
    super.addAttribute(new Attribute({
      key: 'color',
      value: '',
      type: 'kv'
    }));
  }
  get template() {
    const template = `<mat-icon %style% %class% %bCustomProps% %[inline]% %color%>%IconName%</mat-icon>`;
    return template;
  }
  set template(templateString) {}
};