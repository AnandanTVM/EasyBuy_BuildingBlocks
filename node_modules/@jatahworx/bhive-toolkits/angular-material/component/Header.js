'use strict';
let Attribute = require('../../core/Attributes');
let Component = require('../../core/Component');

module.exports = class Header extends Component {
  constructor() {
    const name = 'NG-Header';
    const designerTemplate = `<div class=\"drop\" component-label="Header"></div>`;
    const paletteTemplate = 'Header';
    const componentLabel = 'Header';
    const templateUrl = 'articles/#!components-guide-7/header';

    super({
      name: name,
      designerTemplate: designerTemplate,
      paletteTemplate: paletteTemplate,
      componentLabel: componentLabel,
      templateUrl: templateUrl
    }
      
    );
    super.setType(Component.COMPONENT_TYPE_TITLES.LAYOUT.val);
  }
  get template() {
    const template = `<header %style% %class% %bCustomProps%></header>`;
    return template;
  }
  set template(templateString) {}
};