'use strict';
let Attribute = require('../../core/Attributes');
let Component = require('../../core/Component');

module.exports = class Form extends Component {
  constructor() {
    const name = 'NG-Form';
    const designerTemplate = `<div class=\"drop\" component-label="Form"></div>`;
    const paletteTemplate = 'Form';
    const componentLabel = 'Form';
    const templateUrl = 'articles/#!components-guide-7/form';

    super({
      name,
      designerTemplate,
      paletteTemplate,
      componentLabel,
      templateUrl
    }
     
    );
    super.setType(Component.COMPONENT_TYPE_TITLES.LAYOUT.val);

    super.addAttribute(new Attribute({
      key: 'action',
      value: '',
      type: 'kv'
    }));
    super.addAttribute(new Attribute({
      key: 'method',
      value: '',
      type: 'kv'
    }));
    super.addAttribute(new Attribute({
      key: 'target',
      value: '',
      type: 'kv'
    }));
    super.addAttribute(
      new Attribute({
        key: '(ngSubmit)',
        value: '',
        type: 'kv'
      })
    );
    super.addAttribute(
      new Attribute({
        key: '(reset)',
        value: '',
        type: 'kv'
      })
    );
  }
  get template() {
    const template = `<form %bCustomProps% %style% %class% %action% %method% %(ngSubmit)% %(reset)% %target%></form>`;
    return template;
  }
  set template(templateString) {}
};