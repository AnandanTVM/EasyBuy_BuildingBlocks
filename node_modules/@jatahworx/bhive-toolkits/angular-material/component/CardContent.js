'use strict';
let Attribute = require('../../core/Attributes');
let Component = require('../../core/Component');

module.exports = class CardContent extends Component {
  constructor() {
    const name = 'NG-CardContent';
    const designerTemplate = `<div class=\"drop\" component-label="Card Content"></div>`;
    const paletteTemplate = 'Card Content';
    const componentLabel = 'Card Content';
    const visible = false;
    const templateUrl = 'articles/#!components-guide/card5'; // 'articles/#!components-guide-7/card';

    super({
      name: name,
      designerTemplate: designerTemplate,
      paletteTemplate: paletteTemplate,
      componentLabel: componentLabel,
      templateUrl: templateUrl,
      visible: visible
    }
    );
    super.setType(Component.COMPONENT_TYPE_TITLES.LAYOUT.val);
  }
  get template() {
    const template = `<mat-card-content %style% %class% %bCustomProps%></mat-card-content>`;
    return template;
  }
  set template(templateString) {}
};