"use strict";
let Attribute = require("../../core/Attributes");
let Component = require("../../core/Component");

module.exports = class MissingPackage extends Component {
  constructor() {
    const name = "missing-package";
    const designerTemplate = `<div class=\"ad-header-elements block-drag\" missing-component>
                              </div>`;
    const paletteTemplate = "missing-package";
    const componentLabel = "missing-package";
    const templateUrl = ""; // docs.neutrinos.co
    const visible = false;

    super({
      name: name,
      designerTemplate: designerTemplate,
      paletteTemplate: paletteTemplate,
      componentLabel: componentLabel,
      templateUrl: templateUrl,
      visible: visible
    });

    super.setType(Component.COMPONENT_TYPE_TITLES.OTHERS.val);
  }
  get template() {
    // const template = ``;
    return "";
  }
  set template(templateString) {}
};
