'use strict';
let Component = require('../../core/Component');

module.exports = class List extends Component {
  constructor() {
    const name = 'NG-list';
    const designerTemplate = `<div class=\"drop\" component-label="List"></div>`;
    const paletteTemplate = 'List';
    const componentLabel = 'List';
    const templateUrl = 'articles/#!components-guide-7/list';

    super({
      name: name,
      designerTemplate: designerTemplate,
      paletteTemplate: paletteTemplate,
      componentLabel: componentLabel,
      templateUrl:templateUrl
    });
    super.setType(Component.COMPONENT_TYPE_TITLES.LAYOUT.val);
  }
  get template() {
    const template = `<mat-list %style% %bCustomProps% %class%></mat-list>`;
    return template;
  }
  set template(templateString) {}
};