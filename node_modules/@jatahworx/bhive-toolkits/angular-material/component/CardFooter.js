'use strict';
let Attribute = require('../../core/Attributes');
let Component = require('../../core/Component');

module.exports = class CardFooter extends Component {
  constructor() {
    const name = 'NG-CardFooter';
    const designerTemplate = `<div class=\"drop\" component-label="Card Footer"></div>`;
    const paletteTemplate = 'Card Footer';
    const componentLabel = 'Card Footer';
    const visible = false;
    const templateUrl = 'articles/#!components-guide/card5'; // 'articles/#!components-guide-7/card';

    super({
      name: name,
      designerTemplate: designerTemplate,
      paletteTemplate: paletteTemplate,
      componentLabel: componentLabel,
      templateUrl: templateUrl,
      visible: visible
    }
     
    );
    super.setType(Component.COMPONENT_TYPE_TITLES.LAYOUT.val);
  }
  get template() {
    const template = `<mat-card-footer %bCustomProps% %style% %class%></mat-card-footer>`;
    return template;
  }
  set template(templateString) {}
};