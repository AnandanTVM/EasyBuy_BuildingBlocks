'use strict';
let Attribute = require('../../core/Attributes');
let Component = require('../../core/Component');

module.exports = class CardTitle extends Component {
  constructor() {
    const name = 'NG-CardTitle';
    const designerTemplate = `<div class=\"ad-card-sub-title\">
                                    <span class="component-placeholder">Card Title</span>
                                  </div>`;
    const paletteTemplate = 'Card Title';
    const componentLabel = 'Card Title';
    const visible = false;
    const templateUrl = 'articles/#!components-guide/card5'; // 'articles/#!components-guide-7/card';
    super({
      name: name,
      designerTemplate: designerTemplate,
      paletteTemplate: paletteTemplate,
      componentLabel: componentLabel,
      templateUrl: templateUrl,
      visible: visible
    }
      
    );

    super.addAttribute(
      new Attribute({
        key: 'Title',
        value: '',
        type: 'a',
        useAsLabel: true,
      })
    );
    super.addAttribute(new Attribute({
      key: 'align',
      value: '',
      type: 'kv'
    }));
    super.setType(Component.COMPONENT_TYPE_TITLES.LAYOUT.val);
  }
  get template() {
    const template = `<mat-card-title %bCustomProps% %style% %class% %align%>%Title%</mat-card-title>`;
    return template;
  }
  set template(templateString) {}
};